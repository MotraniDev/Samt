# GitHub Releases update distribution

SAMT will discover updates through a release manifest published with GitHub Releases and will download the approved installer from that release. The application verifies the manifest's SHA-256 hash before launching the installer, retaining explicit user approval and avoiding a separately operated update service.
